<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <dict>
        <key>Label</key>
        <string>my.env</string>
        <key>ProgramArguments</key>
        <array>
            <string>sh</string>
            <string>-c</string>
            <string>
export DOTFILES=$HOME/dotfiles
export VIM_BUNDLE=$HOME/vim-bundle/bundle
export MY_LOCAL=$HOME/local

if [ -d $HOME/vim-runtime ]; then
    export VIMRUNTIME=$HOME/vim-runtime/runtime
else
    version=$(vim --version | head -n 1 | sed -E 's/^[^0-9]*([0-9]+)\.([0-9]+).*/\1\2/')
    fall_back=$(vim --version | grep fall-back | cut -d '"' -f 2)
    export VIMRUNTIME=$fall_back/vim$version
fi

if [ -f $MY_LOCAL/vim_plugin_path ]; then
    export VIM_PLUGIN_PATH=$(cat $MY_LOCAL/vim_plugin_path | tr '\n' ':')
fi

export PATH=$MY_LOCAL/bin:$DOTFILES/bin/darwin:$DOTFILES/bin:$PATH
if [ -d $HOME/bin ]; then
    export PATH=$HOME/bin:$PATH
fi
export PATH="/opt/local/bin:/opt/local/sbin:$PATH"

export EDITOR=vim
export VISUAL=vim

export VIM=$DOTFILES/config/vim
export VIMRUNTIME=$VIMRUNTIME # TODO
export FZF_DEFAULT_OPTS='--reverse'
export GIT_SSL_NO_VERIFY=true

export NIX_PATH=$NIX_PATH:dotfig=$DOTFILES/config
launchctl setenv EDITOR $EDITOR
launchctl setenv VISUAL $VISUAL
launchctl setenv VIM $VIM
launchctl setenv VIMRUNTIME $VIMRUNTIME
launchctl setenv FZF_DEFAULT_OPTS $FZF_DEFAULT_OPTS
launchctl setenv GIT_SSL_NO_VERIFY true $GIT_SSL_NO_VERIFY true
launchctl setenv DOTFILES $DOTFILES
launchctl setenv VIM_BUNDLE $VIM_BUNDLE
launchctl setenv MY_LOCAL $MY_LOCAL
launchctl setenv PATH $PATH
launchctl setenv NIX_PATH $NIX_PATH
            </string>
        </array>
        <key>RunAtLoad</key>
        <true/>
    </dict>
</plist>
