#!/bin/sh

if [ -n "$1" ]; then
    start_node="$1"
else
    start_node=.
fi

file="$(popup 40 30 "fznode $start_node -f -v 3 --extra \\( -type d -o -name '*.pdf' -o -name '*.djvu' \\) -a ! -path '*/\.*'" -title popup)"
if [ -n "$file" ]; then
    zathura "$file"
fi
