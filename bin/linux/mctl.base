#!/bin/sh
echo "[a] choose album"
echo "[s] choose song from current album"
echo
stty -echo
while read -n 1 c; do
    case "$c" in
        a) exec fzalbum ;;
        s) exec fzsong ;;
        *) echo "Command '$c' not recognized. Try again." ;;
    esac
done
