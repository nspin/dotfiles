adduser
visudo
dhcpcd eth0

slackbuilds:
haskell-platform

cabal update

cabal install:
cabal-install
xmonad

TODO:

xmonad-conf, spiral, myLayout? xmobar?
clipboard? (parcellite)
xfc xdefaults stuff?

slackware resolution on vm (semi-) fix:

insert guest addition iso

mount /dev/sr0 /mnt/dvd
cd /mnt/dvd
sh ./VBoxLinuxAdditions.run

cp /etc/X11/xorg.conf-vesta /etc/X11/xorg.conf
vim xorg.conf:

Section "Device"
   Driver    "vboxvideo"
EndSection

Section "Screen"
   Device    "VirtualBox graphics card"
   DefaultDepth    16/24
   SubSection "Display"
     Depth      16
     Modes      "1920x1080"
   EndSubSection
   SubSection "Display"
     Depth      24
     Modes      "1366x768"
   EndSubSection
EndSection

