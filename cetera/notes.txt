addu

visudo

slackbuilds:
ghc
haskell-platform

cabal update

cabal install:
cabal-install
xmonad

TODO:

xmonad-conf, spiral, myLayout?
feh + .fehbg?
clipboard? (parcellite)
xmobar?
pointer?
xfc xdefaults stuff?
yi?
virtualbox guest package?
darcs?


guest additions:

make /etc/X11/xorg.conf (like -vesta, coment all modes)
virtualbox-kernel-addon (pkg install)
mount+install
virtualbox-addon

# mount /dev/sr0 /mnt/dvd
# cd /mnt/dvd
# sh ./VBoxLinuxAdditions.run
