addu

visudo

slackbuilds:
ghc
haskell-platform

cabal update

cabal install:
cabal-install
xmonad

TODO:

xmonad-conf, spiral, myLayout?
feh + .fehbg?
clipboard? (parcellite)
xmobar?
pointer?
xfc xdefaults stuff?
yi?
virtualbox guest package?
darcs?


resolution:

insert guest addition iso

mount /dev/sr0 /mnt/dvd
cd /mnt/dvd
sh ./VBoxLinuxAdditions.run

cp /etc/X11/xorg.conf-vesta /etc/X11/xorg.conf
vim xorg.conf:

Section "Device"
   Identifier   _
   Driver    "vboxvideo"
EndSection

Section "Screen"
   Identifier    _
   Device    "VirtualBox graphics card"
   Monitor    _
   DefaultDepth    24
   SubSection "Display"
     Depth    24
     Modes      "(whatever monitor can handle)"
   EndSubSection
EndSection

